name: Cockroachdb Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "keycloak-crdb-doppler/**"
  pull_request:
    branches:
      - main

jobs:
  build_image:
    name: Build keycloak-crdb image
    uses: ./.github/workflows/docker-publish.yml
    with:
      container_name: keycloak-crdb-doppler
    permissions:
      contents: read
      packages: write
      id-token: write
    secrets: inherit
