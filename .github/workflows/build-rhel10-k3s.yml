name: Build RHEL10 bootc-k3s image
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Build RHEL10 bootc image"]
    types: [completed]
    branches: [main]
  push:
    paths:
      - bootc/rhel10/variant.Containerfile
      - bootc/common/doppler/**
      - bootc/common/tailscale/**
      - bootc/common/kubevirt/**
      - bootc/common/k3s/**
    branches:
      - main

jobs:
  build_bootc:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/build-rhel-bootc.yml
    with:
      image_name: rhel10/rhel-bootc-k3s
      archs: x86_64, arm64
      build-args: |
        DISTRO_VER=10
        LONGHORN=true
      containerfiles: |
        bootc/rhel10/variant.Containerfile
        bootc/common/doppler/Containerfile
        bootc/common/tailscale/Containerfile
        bootc/common/kubevirt/Containerfile
        bootc/common/k3s/Containerfile
    permissions:
      contents: read
      packages: write
    secrets: inherit
