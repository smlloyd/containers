name: Build RHEL9 bootc-gcp image
on:
  workflow_dispatch:
  push:
    paths:
      - bootc/rhel9/Containerfile
      - bootc/common/doppler/**
      - bootc/common/tailscale/**
      - bootc/rhel9/gcp/**
    branches:
      - main

jobs:
  build_bootc:
    uses: ./.github/workflows/build-rhel-bootc.yml
    with:
      image_name: rhel9/rhel-bootc-gcp
      build-args: |
        DISTRO_VER=9
      containerfiles: |
        bootc/rhel9/Containerfile
        bootc/common/doppler/Containerfile
        bootc/common/tailscale/Containerfile
        bootc/rhel9/gcp/Containerfile
    permissions:
      contents: read
      packages: write
    secrets: inherit
